# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# env
.env
.env*.local
# base de préproduction : ses clés ne sont pas celles de .env.local
.env.preprod
.env.preprod.*

# sauvegardes : elles contiennent les données des marchands (et des pièces
# d'identité). Elles ne doivent jamais partir dans le dépôt.
/backups/

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# typescript
*.tsbuildinfo

# design working files (maquettes) — gardés à part
# (décommente si tu ne veux pas versionner les maquettes)
# *.dc.html
# converza-mvp-screens.html
